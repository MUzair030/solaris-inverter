name: threepol_inverter_flutter
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.1.7+18

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter

  dio: ^5.8.0+1
  provider: ^6.1.2
  get_it: ^8.0.3
  fluttertoast: ^8.2.12
  shared_preferences: ^2.5.2
#  device_info: ^2.0.3
#  device_info_plus: ^9.1.2
#  wifi_info_flutter: ^2.0.2
  connectivity_plus: ^5.0.2
  crypto: ^3.0.0
  intl: ^0.19.0
  permission_handler: ^11.3.1
  network_info_plus: ^5.0.3
  http: ^1.2.2
  fl_chart: ^0.63.0
  url_launcher: ^6.1.14
  package_info_plus: ^8.3.0
  share_plus: ^7.2.1
  pin_code_fields: ^8.0.0

  disable_battery_optimization: ^1.1.0
#  flutter_foreground_task: ^9.1.0
  flutter_background_service: ^5.1.0
  flutter_local_notifications: ^17.2.2 # Downgrading to stable version compatible with background service if needed, or update
  android_intent_plus: ^5.0.2
  audioplayers: ^5.2.0 
  flutter_html: ^3.0.0-beta.2
  barcode_widget: ^2.0.4
  qr_flutter: ^4.1.0
  mobile_scanner: ^3.5.2
  wifi_scan: ^0.4.1+2
  app_settings: ^5.1.1
  device_info_plus: ^10.1.0

#  wifi_flutter: ^0.2.0
#  flutter_wifi_connect: ^0.2.0
#  wifi_info_flutter: ^2.0.2
#  flutter_network_connectivity: ^0.0.6
#  wifi_flutter: ^0.2.0

  wifi_iot: ^0.3.18

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0
  flutter_native_splash: ^2.3.2
  flutter_launcher_icons: ^0.13.1

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/Voltis-Logo.png"

#flutter:
#  uses-material-design: true

# For creating flutter default splash screen
flutter_native_splash:
  color: "#0A0A12"
  image: assets/Voltis-Logo-Name-Only.png
  android: true
  fullscreen: true
#  image_scale: 0.7  # Adjust this value (0.5, 0.8, etc.)

  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700

dependency_overrides:
  android_intent_plus:
    git:
      url: https://github.com/fluttercommunity/plus_plugins.git
      path: packages/android_intent_plus

flutter:
  uses-material-design: true

  assets:
    - assets/